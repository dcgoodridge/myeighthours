CREATE TABLE IF NOT EXISTS FICHAJE(ID BIGINT PRIMARY KEY AUTO_INCREMENT NOT NULL, FECHAMARCAJE BIGINT NOT NULL, DIRECCION INTEGER NOT NULL, ESTADO INTEGER NOT NULL, TIPO INTEGER NOT NULL, TERMINAL INTEGER NOT NULL);